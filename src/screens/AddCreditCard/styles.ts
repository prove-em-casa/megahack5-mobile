import styled from 'styled-components/native';
import { DefaultButton } from '../../styles/global';

export const AddCreditCardContainer = styled.View`
  flex: 1;
  justify-content: center;
  align-items: center;
`;

export const AddCreditCardButton = styled(DefaultButton)`
  margin-top: 20px;
`;
